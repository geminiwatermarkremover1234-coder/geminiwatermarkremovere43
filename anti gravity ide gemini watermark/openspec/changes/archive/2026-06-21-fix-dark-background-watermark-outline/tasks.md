## 1. Core Processor Enhancement

- [ ] 1.1 Implement dynamic `smoothStrengthMap` calculation in `cleanFrameWatermark` of `processor.js`.
- [ ] 1.2 Update the edge cleanup phase in `cleanFrameWatermark` of `processor.js` to utilize the dynamic smoothing strength map.

## 2. Verification and testing

- [ ] 2.1 Run the Python dynamic cleaning script and mathematical erasure check.
- [ ] 2.2 Execute browser subagent tests to upload, process, zoom, and capture screenshots of watermark removal on dark background.
